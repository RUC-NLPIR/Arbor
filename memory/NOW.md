# Current State

## Mission

Implement and validate the simplest Agent-centric AROS, then retire Arbor only
after equivalent public capabilities are commissioned.

## Current position

- Principal owns the scientific outer loop; Researcher Task owns one direction's
  inner loop. AROS owns workspace, Git/worktree, process, Eval receipts, and
  restart Attention.
- Model-visible AROS tools are `Attention`, `Task`, `Run`, `Eval`, and optionally
  host-granted `Checkpoint`.
- Task now owns brief/worktree/mailbox/return/collection/preserve/prune but no
  process carrier. One idempotent trusted-local Run owns its process/log/stop/
  timeout/lost/descendant truth; collection binds TaskRun manifest/final and B-C-R
  return lineage, and Principal observes both Run final and collection.
- Checkpoint is ordinary selected-path Git. Host-observed Task/Run/Eval refs are
  automatic `AROS-Observed:` trailers; scientific meaning remains Markdown.
- Proposal, admission, assimilation, semantic index, strict Evidence relation,
  rebuild commands, bridges, compatibility readers, and dual writes are deleted.
- Deterministic clean-wheel E2E completed preregistration, one Task, one valid
  Eval, final Question/Model/Idea/Claim/NOW prose, primary destruction, and a
  fresh Attention restart with `unread_returns=[]`. Exact current evidence is
  `docs/analysis/aros-simple-loop-smoke.md` at product source `4f1eb3d`, evidence
  SHA-256 `b5f35280d05e24f528d337b3916f6d72a3d394beb526e7fa06febcae57c96820`,
  and final research commit `7ca93fc634d08e4598016f7b4e9ad2fd276e4c57`.
- Current product source `26fe611fc88252a4667d24b0db92b742f654712e`
  separately fixes the post-commission stop/final publication race. Its clean-wheel
  full gate collected 1,964 tests: 1,958 passed, 6 skipped, 0 failed, exit 0; the
  adopted-descendant stop test passed 20/20 consecutive runs.
- Phase 0A is complete under the human-approved interim gate: recursive `src/aros`
  was exactly 17,700 LOC at Task 8 evidence and is currently 17,697 after the
  race fix; the duplicate carrier is absent. Phase 0B must still reach the original
  program-wide `src/aros <= 12,000 LOC` gate before Phase A Mission Supervisor.
  Functionality remains intentionally limited.

## Current limits

- Cooperative same-UID Git is not protected authority.
- Generic Run drains same-PGID and live-subreaper adopted descendants. Public stop
  after leader exit requires the exact bound live local carrier, and uncontained
  descendants cannot produce a clean final.
- Mode-normalizing filesystem and controlled Git optional-lock behavior are facts,
  not protected authority.
- A real external-model Researcher inner loop and async portfolio are not yet
  commissioned.
- Source Gateway, Independent Reviewer, project Skills, MCP parity, Mission
  Supervisor/shared budgets, protected authority, complete K/M/G productization,
  and Arbor retirement remain open. Current evidence makes no “strictly better
  than Arbor” claim.

## Next obligations

1. Preserve the Task-on-Run E2E evidence and keep full repository/focused AROS
   gates green.
2. Complete Phase 0B `src/aros <= 12,000 LOC` before Mission Supervisor work.
3. Commission one real Researcher inner loop before adding async concurrency.
4. Add Skills or MCP only after a repeated native-service need is measured.
